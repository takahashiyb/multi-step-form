import { describe, it } from 'node:test'

describe('test main section', () => {
  it.todo('check click next step button with success from start to end', () => {
    //// Go to step 4
    // expect('in step 4')
    //// click Go back button
    // expect('in step 3')
    //// click Go back button
    // expect('in step 2')
    //// click Go back button
    // expect('in step 1')
  })
  it.todo('check click go back button from end to start', () => {
    //// complete data
    //// Go to step 1
    // expect('in step 1')
    //// click Go back button
    // expect('in step 2')
    //// click Go back button
    // expect('in step 3')
    //// click Go back button
    // expect('in step 4')
  })
})
