import { describe, it } from 'node:test'

describe('test confirmation and completion step', () => {
  it.todo('check click change button', () => {
    //// click change button
    // expect('goes to step 2')
  })
  it.todo('check confirm button with success', () => {
    //// complete data
    //// click confirm button
    // expect('confirm success')
  })
  it.todo('check confirm button with missing details', () => {
    //// randomize index change data to null
    //// click confirm button
    // expect('confirm unsuccessful')
  })
  it.todo('check confirm button with wrong email', () => {
    //// change email data to wrong format
    //// click confirm button
    // expect('confirm unsuccessful')
  })
})
