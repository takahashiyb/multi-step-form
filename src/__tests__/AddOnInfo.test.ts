import { describe, it } from 'node:test'

describe('test Plan info step', () => {
  it.todo('selecting and deselecting addons', () => {
    //// select Add on 1 data
    // expect('the Add on 1 selected')
    // expect('the Add on 2 unselected')
    //// select Add on 2 data
    // expect('the Add on 1 selected')
    // expect('the Add on 2 selected')
    //// deselect a Add on 1 data
    // expect('the Add on 1 deselected')
    // expect('the Add on 2 selected')
  })
  it.todo('selecting all addons', () => {
    //// selecting all Add ons
    // expect('submit successful')
    // expect('all add on data selected')
  })
  it.todo('selecting no addons', () => {
    //// selecting no Add ons
    // expect('submit successful')
    // expect('no add on data selected')
  })
})
