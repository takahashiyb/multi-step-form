import { describe, it } from 'node:test'

describe('test Plan info step', () => {
  it.todo('check missing tier selection error message', () => {
    //// submit with no tier data
    // expect('error message appearing')
    // expect('no submit event')
    //// select plan data
    // expect('no error code')
    // expect('submit event successful')
  })
  it.todo('check tier selection switching', () => {
    //// select a tier data
    // expect('the correct tier selected')
    // expect('the next tier unselected')
    //// select a different tier data
    // expect('the correct tier selected')
    // expect('the previous tier deselected')
    //// select a different tier data
    // expect('the correct tier selected')
    // expect('the previous tier deselected')
  })
  it.todo('check plan selection switching', () => {
    //// select a plan data
    // expect('the correct plan selected')
    // expect('the next plan unselected')
    //// select a different plan data
    // expect('the correct plan selected')
    // expect('the previous plan deselected')
    //// select a different plan data
    // expect('the correct plan selected')
    // expect('the previous plan deselected')
  })
})
